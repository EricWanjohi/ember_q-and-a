
import { noBookmark } from 'ember-q-and-a/helpers/no-bookmark';
import { module, test } from 'qunit';

module('Unit | Helper | no bookmark');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noBookmark([42]);
  assert.ok(result);
});

