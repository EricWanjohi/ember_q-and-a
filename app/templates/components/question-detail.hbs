<div class="question">
  <hr>
  <div class="center">
    <h3>{{question.question}}</h3>
    <p>Asked by: {{question.author}}</p>
  </div>
  <p>{{question.notes}}</p>

  <h5>Location:</h5>
  <p>{{question.city}}</p>
  <button class="btn btn-sm bookmark-button" {{action 'addToBookmarks' question}}>Bookmark Question</button>
  <hr>
  <div class="center">
    <h4>Answers from the Vampire Community</h4>
  </div>
  <hr>
  {{no-answer question}}
  <p>
    {{#each question.answers as |answer|}}
      {{answer-detail answer=answer destroyAnswer="destroyAnswer"}}
    {{/each}}
  </p>
</div>

<button class="btn btn-sm floating-button" {{action 'delete' question}}>Delete Question</button>
