<div class="question">
  {{new-question saveQuestion="saveQuestion"}}

  {{#each model as |question|}}
    <div class="center">
      <hr>{{question-preview question=question}}
    </div>
  {{/each}}
</div>
